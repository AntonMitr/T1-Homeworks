spring:
  application:
    name: clientProcessing

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yml

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost:5432}/client_processing
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver